import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class E26Test {

    @Test
    public static void main(String[] args) {

        int high = 100;
        int low = 1;
        assertTrue("Error, apr is too high", high < apr);
        assertTrue("Error, apr is too low",  low  > apr);

    }
}